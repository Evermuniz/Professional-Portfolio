import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// this is what finally renders everything to the div with the #root id in the index.html file

ReactDOM.render(<App />, document.getElementById('root'));